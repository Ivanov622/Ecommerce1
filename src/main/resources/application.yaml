spring:
  h2:
    console:
      enabled: 'true'
  datasource:
    url: jdbc:h2:mem:test
  application:
    name: ecom-application
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
server:
  port: '8081'